import { StyleSheet } from 'react-native';

export const Fonts = StyleSheet.create({
  title: {
    fontSize: 20,
    letterSpacing: 0,
  },
  caption: {
    fontSize: 15,
    letterSpacing: 0,
    fontStyle: 'italic',
  },
});
